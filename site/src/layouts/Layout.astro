---
import '../styles/globals.css';
import { SITE_URL, VERSION } from '../constants';

export interface Props {
  title: string;
  description?: string;
  image?: string;
  canonical?: string;
}

const {
  title,
  description = "Prevent postinstall scripts from stealing your secrets. Compromised packages install normally — but SSH keys, cloud credentials, and .env files stay hidden inside a bwrap sandbox.",
  image = "/og-default.png",
  canonical = SITE_URL,
} = Astro.props;

const siteTitle = title.includes("ringfence") ? title : `${title} — ringfence`;
const imageUrl = new URL(image, canonical).href;
---

<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{siteTitle}</title>
    <meta name="description" content={description} />
    <meta name="author" content="Alexander Smyshlyaev" />
    <meta name="color-scheme" content="dark" />
    <meta name="theme-color" content="oklch(0.115 0.006 60)" />
    <meta name="robots" content="index, follow" />
    <link rel="canonical" href={canonical} />

    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="ringfence" />
    <meta property="og:title" content={siteTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={imageUrl} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:alt" content="ringfence — prevent postinstall scripts from stealing your secrets" />
    <meta property="og:locale" content="en_US" />

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={siteTitle} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={imageUrl} />
    <meta name="twitter:image:alt" content="ringfence — prevent postinstall scripts from stealing your secrets" />

    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Bricolage+Grotesque:opsz,wght@12..96,200..800&family=JetBrains+Mono:wght@400;500;600;700&display=swap" rel="stylesheet" />
    <meta name="google-site-verification" content="VGWeNcrEVDQA07xz1L_6VZjcMEip0kTWdxxpIEmmbKc" />

    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "SoftwareApplication",
      name: "ringfence",
      applicationCategory: "DeveloperApplication",
      applicationSubCategory: "SecurityTool",
      operatingSystem: "Linux, macOS",
      description: "Prevent postinstall scripts from stealing secrets during npm/pnpm/yarn/bun installs. Uses bwrap (Linux) or Docker (macOS) to sandbox installs. Compromised packages can't exfiltrate what they can't see.",
      url: SITE_URL,
      keywords: "prevent secrets theft, npm security, supply chain security, sandbox npm install, bubblewrap, postinstall protection, exfiltration protection, open source security",
      softwareVersion: VERSION,
      offers: {
        "@type": "Offer",
        price: "0",
        priceCurrency: "USD",
      },
      author: {
        "@type": "Person",
        name: "Alexander Smyshlyaev",
        url: "https://asmyshlyaev177.dev",
        sameAs: "https://github.com/asmyshlyaev177",
      },
    })} />

    <!-- Google tag (gtag.js) -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-5RDPXF8V0F"></script>
  <script>
    window.dataLayer = window?.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());

    gtag('config', 'G-5RDPXF8V0F');
  </script>

  </head>
  <body class="bg-base text-body antialiased">
    <slot />
  </body>
</html>
